[package]
name = "procfastd"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Daemon that loads procfast BPF programs and pins maps for shared access"

[dependencies]
procfast = { path = "../procfast" }
procfast-common = { path = "../procfast-common" }
log = "0.4"
env_logger = "0.11"
signal-hook = "0.3"

[package]
name = "procfast-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared type definitions for procfast BPF maps"

[dependencies]
bytemuck = { version = "1", features = ["derive"] }

[features]
default = []
